# 📧 Hostinger SMTP - ملفات منفصلة للاختبار

## 📁 الملفات الجديدة:

### 1. `config-hostinger.php`
- ملف إعدادات منفصل لـ Hostinger SMTP
- لا يتداخل مع الإعدادات الحالية
- سجل منفصل: `email_log_hostinger.txt`

### 2. `send-email-hostinger.php`
- ملف إرسال منفصل ومبسط
- يستخدم `config-hostinger.php`
- كود نظيف بدون تعقيدات OAuth2

### 3. `test-hostinger-smtp.php`
- ملف اختبار الاتصال
- يعرض تفاصيل الاتصال والأخطاء

---

## 🚀 كيفية الاستخدام:

### الخطوة 1: اختبار الاتصال
افتح في المتصفح:
```
https://yasrendezvous.ae/test-hostinger-smtp.php
```

سيظهر:
- ✅ نجاح الاتصال والمصادقة
- ❌ أو تفاصيل الخطأ

### الخطوة 2: تحديث النموذج
تم تحديث `index.html` لاستخدام:
- `send-email-hostinger.php` بدلاً من `send-email-smtp.php`

### الخطوة 3: اختبار الإرسال
1. املأ النموذج في الموقع
2. اضغط Submit
3. تحقق من `email_log_hostinger.txt`

---

## ⚙️ الإعدادات الحالية:

```php
SMTP_HOST: smtp.hostinger.com
SMTP_PORT: 587
SMTP_SECURE: tls
SMTP_USERNAME: no-reply@yasmarina.ae
SMTP_PASSWORD: M[1t~/zhgkl
```

---

## 🔍 إذا استمرت المشكلة:

### 1. تحقق من الحساب في Hostinger:
- تسجيل الدخول إلى Hostinger Control Panel
- Email Accounts → no-reply@yasmarina.ae
- تأكد من:
  - ✅ الحساب نشط
  - ✅ كلمة المرور صحيحة
  - ✅ SMTP مفعل

### 2. جرب منفذ آخر:
في `config-hostinger.php`، غيّر:
```php
define('SMTP_PORT', 465);
define('SMTP_SECURE', 'ssl');
```

### 3. تحقق من كلمة المرور:
- قد تحتوي على أحرف خاصة تسبب مشاكل
- جرب إعادة تعيين كلمة المرور في Hostinger
- استخدم كلمة مرور بسيطة للاختبار

---

## 📝 ملفات السجل:

- `email_log_hostinger.txt` - سجل الملف الجديد
- `email_log.txt` - سجل الملف القديم

---

## ✅ المميزات:

1. **منفصل تماماً** - لا يؤثر على الإعدادات الحالية
2. **مبسط** - كود نظيف بدون تعقيدات
3. **سهل التشخيص** - سجل منفصل وواضح
4. **قابل للاختبار** - ملف اختبار منفصل

---

**ملاحظة:** الملفات الجديدة جاهزة للاستخدام. جرب `test-hostinger-smtp.php` أولاً لاختبار الاتصال.

