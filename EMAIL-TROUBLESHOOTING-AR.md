# 🔧 دليل حل مشاكل البريد الإلكتروني

## المشكلة الحالية

تظهر رسالة الخطأ:
```
An error occurred while sending the message. Please try again.
```

---

## ✅ الحل السريع (خطوات عملية)

### الخطوة 1: إنشاء حساب البريد الإلكتروني ⭐ **مهم جداً**

**يجب** إنشاء حساب البريد في cPanel أولاً:

1. ادخل على **cPanel** الخاص بالاستضافة
2. ابحث عن **Email Accounts** (حسابات البريد)
3. اضغط على **Create** (إنشاء)
4. املأ البيانات:
   ```
   Email: no-reply
   Domain: yasmarina.ae
   Password: [اختر كلمة مرور قوية]
   ```
5. اضغط **Create Account**

> **ملاحظة مهمة:** البريد يجب أن يكون من نفس الدومين (yasmarina.ae)

---

### الخطوة 2: استخدام أداة التشخيص

1. ارفع الملفات المحدثة إلى السيرفر
2. افتح المتصفح واذهب إلى:
   ```
   https://yourwebsite.com/diagnose-email.php
   ```
3. ستظهر لك أداة التشخيص بواجهة جميلة
4. اضغط على زر **"🚀 Run Email Tests"**
5. ستقوم الأداة بإجراء 3 اختبارات مختلفة

**النتائج المتوقعة:**
- ✅ إذا نجح أي اختبار = المشكلة محلولة! تحقق من بريدك
- ❌ إذا فشلت كل الاختبارات = انتقل للخطوة 3

---

### الخطوة 3: فحص السجل (Log File)

1. افتح ملف `email_log.txt` على السيرفر
2. ستجد تفاصيل دقيقة عن سبب فشل الإرسال
3. ابحث عن آخر سطر يحتوي على "Details:"

**أمثلة على الأخطاء:**
```
Method 1 failed (with -f parameter). Method 2 failed (without -f parameter). Method 3 failed (simple headers).
```
هذا يعني أن دالة `mail()` لا تعمل على السيرفر.

---

### الخطوة 4: الحلول البديلة

#### الحل 1: التواصل مع الدعم الفني

اتصل بالدعم الفني للاستضافة واطلب:

```
مرحباً،

أحتاج تفعيل دالة mail() في PHP على حسابي.
حالياً لا أستطيع إرسال البريد الإلكتروني من السكريبت.

Domain: yasmarina.ae

شكراً لكم
```

#### الحل 2: استخدام SMTP (الأفضل والأكثر موثوقية)

إذا لم يعمل `mail()`، استخدم SMTP:

**خطوات تفعيل SMTP:**

1. **تحميل PHPMailer** (إذا لم يكن موجوداً):
   - حمّل من: https://github.com/PHPMailer/PHPMailer/archive/refs/heads/master.zip
   - استخرج الملفات في مجلد `PHPMailer`

2. **تحديث ملف index.html**:
   
   ابحث عن السطر:
   ```html
   action="send-email-enhanced.php"
   ```
   
   غيّره إلى:
   ```html
   action="send-email-smtp.php"
   ```

3. **تحديث إعدادات SMTP في config.php**:
   
   ```php
   // SMTP Settings
   define('SMTP_HOST', 'mail.yasmarina.ae'); // أو smtp.gmail.com
   define('SMTP_PORT', 587);
   define('SMTP_USERNAME', 'no-reply@yasmarina.ae');
   define('SMTP_PASSWORD', 'your-password-here'); // كلمة المرور للبريد
   define('SMTP_SECURE', 'tls');
   ```

4. **احصل على إعدادات SMTP من cPanel**:
   - في cPanel، اذهب إلى **Email Accounts**
   - اضغط على **Connect Devices** بجانب البريد
   - ستجد إعدادات SMTP:
     - **Outgoing Server**: mail.yourdomain.com
     - **Port**: عادة 587 أو 465
     - **Username**: no-reply@yasmarina.ae
     - **Password**: كلمة المرور التي أنشأتها

#### الحل 3: استخدام Gmail SMTP (للتجربة فقط)

إذا أردت تجربة سريعة:

1. أنشئ حساب Gmail
2. فعّل "App Passwords" من إعدادات الأمان
3. في `config.php`:
   ```php
   define('SMTP_HOST', 'smtp.gmail.com');
   define('SMTP_PORT', 587);
   define('SMTP_USERNAME', 'youremail@gmail.com');
   define('SMTP_PASSWORD', 'your-app-password'); // ليس كلمة المرور العادية
   define('SMTP_SECURE', 'tls');
   ```

---

## 🔍 تشخيص المشكلة

### أسباب محتملة للمشكلة:

1. **البريد غير موجود في cPanel** ❌
   - الحل: أنشئ البريد `no-reply@yasmarina.ae`

2. **دالة mail() معطلة على السيرفر** ⚠️
   - الحل: تواصل مع الدعم الفني أو استخدم SMTP

3. **معامل -f محظور** 🚫
   - الكود المحدث يحاول 3 طرق مختلفة تلقائياً

4. **Sendmail غير مُعد بشكل صحيح** ⚙️
   - الحل: استخدم SMTP بدلاً من mail()

5. **البريد يذهب إلى Spam** 📧
   - الحل: تحقق من مجلد Spam/Junk

---

## 📊 التحديثات التي تمت

### ملف `send-email-enhanced.php` المُحدّث:

الآن يحاول **3 طرق مختلفة** تلقائياً:

1. ✅ **الطريقة 1**: مع معامل `-f`
2. ✅ **الطريقة 2**: بدون معامل `-f`
3. ✅ **الطريقة 3**: headers بسيطة جداً

**إذا نجحت أي طريقة = البريد سيُرسل!**

### ميزات إضافية:

- 📝 **Logging مُحسّن**: يسجل سبب الفشل بالتفصيل
- 🔍 **تتبع الأخطاء**: يحفظ آخر خطأ من PHP
- 🔄 **محاولات متعددة**: لا يستسلم بسرعة
- 🇬🇧 **رسائل بالإنجليزية**: كما طلبت

---

## 📝 سجل التغييرات (Changelog)

### الإصدار 2.0 - 5 نوفمبر 2025

✅ **تم إصلاح:**
- تغيير From header ليستخدم بريد من نفس الدومين
- إضافة 3 محاولات مختلفة لإرسال البريد
- تحسين الـ logging
- تحويل جميع الرسائل للإنجليزية

✅ **تم إضافة:**
- أداة تشخيص متقدمة (diagnose-email.php)
- دعم أفضل لـ shared hosting
- معالجة أخطاء محسّنة

---

## ⚠️ تحذيرات أمنية

### ملفات يجب حذفها بعد الاختبار:

1. ❌ `diagnose-email.php` - **احذفه فوراً بعد الاختبار!**
2. ❌ `test-email.php` - **احذفه أيضاً!**

هذه الملفات تعرض معلومات حساسة ولا يجب أن تكون متاحة للعامة.

---

## 🎯 خطة العمل الموصى بها

### 1. الآن - فوراً:

```bash
☐ إنشاء حساب البريد no-reply@yasmarina.ae في cPanel
☐ رفع الملفات المحدثة
☐ فتح diagnose-email.php
☐ تشغيل الاختبارات
```

### 2. إذا نجحت الاختبارات:

```bash
☐ تحقق من البريد الوارد (والـ Spam)
☐ جرّب النموذج على الموقع
☐ احذف diagnose-email.php
☐ احذف test-email.php
```

### 3. إذا فشلت كل الاختبارات:

```bash
☐ انسخ سجل الأخطاء من email_log.txt
☐ اتصل بالدعم الفني (أو أرسل لي السجل)
☐ جهّز PHPMailer للاستخدام
☐ غيّر إلى SMTP في index.html
```

---

## 📞 تحتاج مساعدة إضافية؟

إذا جربت كل الحلول أعلاه ولم ينجح شيء:

1. افتح `email_log.txt` وانسخ آخر 10 أسطر
2. افتح `diagnose-email.php` واعمل screenshot للنتائج
3. أرسل لي:
   - نتائج الاختبارات
   - محتوى السجل
   - اسم شركة الاستضافة

---

## 🚀 ملاحظة نهائية

معظم مشاكل البريد على shared hosting تُحل بواحدة من طريقتين:

1. ✅ **إنشاء حساب البريد في cPanel** (90% من الحالات)
2. ✅ **استخدام SMTP بدلاً من mail()** (100% نجاح)

**الحل الأفضل:** استخدم SMTP مع PHPMailer - أكثر موثوقية واحترافية!

---

**آخر تحديث:** 5 نوفمبر 2025  
**الإصدار:** 2.0  
**الحالة:** ✅ جاهز للاختبار

