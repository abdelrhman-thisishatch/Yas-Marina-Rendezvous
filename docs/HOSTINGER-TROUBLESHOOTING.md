# 🔧 حل مشكلة المصادقة مع Hostinger SMTP

## ❌ الخطأ الحالي:
```
SMTP Error: Could not authenticate.
```

## 🔍 التشخيص:

هذا الخطأ يعني:
- ✅ الاتصال بالسيرفر نجح
- ❌ المصادقة فشلت (اسم المستخدم أو كلمة المرور)

---

## ✅ الحلول الممكنة:

### الحل 1: التحقق من بيانات الاعتماد في Hostinger

1. **تسجيل الدخول إلى Hostinger:**
   - اذهب إلى: https://hpanel.hostinger.com
   - أو: https://www.hostinger.com/cpanel

2. **التحقق من حساب البريد:**
   - اذهب إلى **Email** → **Email Accounts**
   - ابحث عن: `no-reply@yasmarina.ae`
   - تأكد من:
     - ✅ الحساب موجود ونشط
     - ✅ كلمة المرور صحيحة
     - ✅ SMTP مفعل

3. **اختبار تسجيل الدخول:**
   - جرب تسجيل الدخول بحساب البريد من:
     - Webmail: https://webmail.hostinger.com
   - إذا لم تستطع تسجيل الدخول، كلمة المرور خاطئة

---

### الحل 2: إعادة تعيين كلمة المرور

1. **في Hostinger Control Panel:**
   - Email → Email Accounts
   - اختر `no-reply@yasmarina.ae`
   - اضغط **Change Password**

2. **استخدم كلمة مرور بسيطة:**
   - تجنب الأحرف الخاصة المعقدة: `[`, `]`, `~`, `/`
   - استخدم: أحرف وأرقام فقط
   - مثال: `Password123` أو `YasMarina2025`

3. **حدث config-hostinger.php:**
   ```php
   define('SMTP_PASSWORD', 'كلمة المرور الجديدة');
   ```

---

### الحل 3: التحقق من اسم المستخدم

**يجب أن يكون:**
- ✅ `no-reply@yasmarina.ae` (البريد الكامل)
- ❌ ليس `no-reply` فقط

**في config-hostinger.php:**
```php
define('SMTP_USERNAME', 'no-reply@yasmarina.ae'); // ✅ صحيح
```

---

### الحل 4: تجربة منافذ مختلفة

#### Option A: Port 587 with TLS (الأكثر توافقاً)
```php
define('SMTP_PORT', 587);
define('SMTP_SECURE', 'tls');
```

#### Option B: Port 465 with SSL
```php
define('SMTP_PORT', 465);
define('SMTP_SECURE', 'ssl'); // ⚠️ أحرف صغيرة!
```

---

### الحل 5: التحقق من إعدادات SMTP في Hostinger

بعض مزودي الاستضافة يحتاجون تفعيل SMTP يدوياً:

1. **في Hostinger Control Panel:**
   - Email → Email Accounts
   - اختر الحساب
   - ابحث عن خيار **SMTP** أو **Outgoing Mail**
   - تأكد من أنه **مفعل**

2. **إذا لم تجد الخيار:**
   - اتصل بدعم Hostinger
   - اسأل عن تفعيل SMTP للحساب

---

## 🧪 خطوات الاختبار:

### 1. اختبار الاتصال:
افتح: `https://yasrendezvous.ae/test-hostinger-smtp.php`

### 2. فحص السجل:
تحقق من: `email_log_hostinger.txt`

### 3. اختبار تسجيل الدخول:
- جرب تسجيل الدخول من Webmail
- إذا نجح، المشكلة في الكود
- إذا فشل، المشكلة في كلمة المرور

---

## 📋 قائمة التحقق:

قبل الاتصال بدعم Hostinger، تأكد من:

- [ ] الحساب `no-reply@yasmarina.ae` موجود في Hostinger
- [ ] الحساب نشط (Active)
- [ ] كلمة المرور صحيحة (جرب تسجيل الدخول من Webmail)
- [ ] اسم المستخدم هو البريد الكامل: `no-reply@yasmarina.ae`
- [ ] SMTP مفعل للحساب (إن أمكن)
- [ ] جربت المنفذين: 587 و 465
- [ ] جربت كلمة مرور بسيطة (بدون أحرف خاصة)

---

## 🔐 ملاحظات حول كلمة المرور:

### الأحرف الخاصة التي قد تسبب مشاكل:
- `[` و `]` - أقواس مربعة
- `~` - تيلدا
- `/` - شرطة مائلة
- `\` - شرطة مائلة عكسية
- `&` - علامة و
- `%` - علامة نسبة

### كلمة المرور الحالية تحتوي على:
- `M[1t~/zhgkl` - تحتوي على `[`, `~`, `/`

**الحل:** استخدم كلمة مرور بسيطة للاختبار:
```php
define('SMTP_PASSWORD', 'YasMarina2025'); // مثال
```

---

## 📞 الاتصال بدعم Hostinger:

إذا استمرت المشكلة:

1. **اتصل بدعم Hostinger:**
   - اشرح أنك تحاول استخدام SMTP
   - اذكر الحساب: `no-reply@yasmarina.ae`
   - اسأل عن:
     - هل SMTP مفعل للحساب؟
     - ما هي الإعدادات الصحيحة؟
     - هل هناك قيود على SMTP؟

2. **اطلب منهم:**
   - تفعيل SMTP للحساب
   - إعادة تعيين كلمة المرور
   - تأكيد الإعدادات الصحيحة

---

## 🎯 الخطوات التالية:

1. ✅ جرب إعادة تعيين كلمة المرور في Hostinger
2. ✅ استخدم كلمة مرور بسيطة (أحرف وأرقام فقط)
3. ✅ حدث `config-hostinger.php`
4. ✅ جرب الإرسال مرة أخرى
5. ✅ إذا استمرت المشكلة، اتصل بدعم Hostinger

---

**آخر تحديث:** تم إصلاح مشكلة SMTP_SECURE (يجب أن يكون بأحرف صغيرة)

