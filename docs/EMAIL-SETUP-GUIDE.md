# دليل إعداد البريد الإلكتروني - Yas Marina Rendezvous

## 🔧 المشكلة وتم حلها

تم تحديث ملفات البريد الإلكتروني لتعمل على shared hosting. المشكلة الأساسية كانت أن الـ `From` header يستخدم بريد المستخدم، وهذا غير مسموح على معظم السيرفرات المشتركة.

## ✅ ما تم تحديثه

### 1. ملف `send-email-enhanced.php`
- ✅ تم تغيير `From` header ليستخدم `no-reply@yasmarina.ae`
- ✅ تم إضافة MIME headers إضافية
- ✅ تم إضافة معامل خامس لدالة `mail()`
- ✅ الـ `Reply-To` لا يزال يستخدم بريد المستخدم للرد المباشر

### 2. ملف `send-email-smtp.php`
- ✅ تم تحديثه أيضاً كنسخة احتياطية
- ✅ يدعم PHPMailer إذا فشلت دالة `mail()`

### 3. ملف اختبار جديد `test-email.php`
- ✅ ملف لاختبار إعدادات البريد على السيرفر

---

## 📋 خطوات التفعيل (مهمة جداً!)

### الخطوة 1: إنشاء حساب بريد إلكتروني

**يجب** إنشاء حساب بريد إلكتروني في cPanel:

```
البريد: no-reply@yasmarina.ae
```

#### كيفية إنشاء الحساب:
1. ادخل على cPanel
2. اذهب إلى **Email Accounts**
3. اضغط على **Create**
4. أدخل:
   - Email: `no-reply`
   - Domain: `yasmarina.ae`
   - Password: أي كلمة مرور قوية
5. اضغط **Create**

---

### الخطوة 2: اختبار الإعدادات

1. ارفع الملفات المحدثة إلى السيرفر
2. افتح المتصفح وانتقل إلى:
   ```
   https://yourdomain.com/test-email.php
   ```
3. تحقق من النتائج:
   - ✅ إذا ظهر "تم إرسال البريد بنجاح" - تحقق من البريد الوارد
   - ❌ إذا فشل - اتبع الخطوة 3

---

### الخطوة 3: إذا لم يعمل

#### الحل 1: تحقق من البريد المزعج (Spam)
- البريد قد يصل إلى Spam في أول مرة
- تحقق من مجلد Junk/Spam

#### الحل 2: تواصل مع الدعم الفني
اطلب من الدعم الفني للسيرفر:
```
"الرجاء التأكد من أن دالة mail() مفعلة وأنها تعمل بشكل صحيح على حسابي"
```

#### الحل 3: استخدم SMTP (الأفضل)
إذا استمرت المشكلة، استخدم PHPMailer مع SMTP:

1. حمّل PHPMailer:
   ```bash
   composer require phpmailer/phpmailer
   ```
   أو حمّلها يدوياً من: https://github.com/PHPMailer/PHPMailer

2. ضعها في مجلد `PHPMailer`

3. في `index.html` غيّر:
   ```html
   action="send-email-enhanced.php"
   ```
   إلى:
   ```html
   action="send-email-smtp.php"
   ```

4. حدّث إعدادات SMTP في `config.php`:
   ```php
   define('SMTP_HOST', 'mail.yasmarina.ae'); // أو smtp.gmail.com
   define('SMTP_PORT', 587);
   define('SMTP_USERNAME', 'no-reply@yasmarina.ae');
   define('SMTP_PASSWORD', 'your-password-here');
   ```

---

## 🔍 معلومات تقنية

### التغييرات الرئيسية:

**قبل التحديث:**
```php
$headers = "From: " . $email . "\r\n"; // ❌ يستخدم بريد المستخدم
```

**بعد التحديث:**
```php
$fromEmail = "no-reply@yasmarina.ae";
$headers = "From: Yas Marina Rendezvous <" . $fromEmail . ">\r\n"; // ✅ يستخدم بريد من نفس الدومين
$headers .= "Reply-To: " . $email . "\r\n"; // المستخدم يمكنه الرد
```

### لماذا هذا التغيير مهم؟
1. **SPF Records**: السيرفر يتحقق من أن البريد المرسل من دومين صحيح
2. **DMARC Policy**: معظم السيرفرات لا تسمح بإرسال بريد من دومين خارجي
3. **Anti-Spam**: منع استخدام النظام للـ spam

---

## ⚠️ تحذير أمني

**احذف ملف `test-email.php` بعد الانتهاء من الاختبار!**

هذا الملف يعرض معلومات حساسة عن السيرفر.

---

## 📞 الدعم

إذا واجهت أي مشكلة:
1. تحقق من أن البريد `no-reply@yasmarina.ae` موجود
2. تحقق من log file: `email_log.txt`
3. تواصل مع الدعم الفني للسيرفر

---

## ✨ المزايا الحالية

الملفات المحدثة تتضمن:
- ✅ حماية من CSRF
- ✅ Rate limiting (10 رسائل في الساعة)
- ✅ تسجيل جميع العمليات (Logging)
- ✅ حماية من HTML Injection
- ✅ التحقق من صحة البريد الإلكتروني
- ✅ دعم UTF-8 للغة العربية

---

**آخر تحديث:** 5 نوفمبر 2025

