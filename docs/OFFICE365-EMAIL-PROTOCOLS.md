# 📧 بروتوكولات البريد الإلكتروني في Office 365

## ⚠️ ملاحظة مهمة:

**IMAP** و **POP** هما بروتوكولات لاستقبال البريد الإلكتروني فقط (Incoming Mail)  
**SMTP** هو البروتوكول الوحيد لإرسال البريد الإلكتروني (Outgoing Mail)

---

## 📥 بروتوكولات الاستقبال (Incoming Mail):

### IMAP (Internet Message Access Protocol)
- **الاستخدام**: استقبال وقراءة البريد الإلكتروني
- **الميزة**: البريد يبقى على السيرفر
- **المنفذ**: 993 (SSL/TLS) أو 143 (غير مشفر)

### POP (Post Office Protocol)
- **الاستخدام**: استقبال وتنزيل البريد الإلكتروني
- **الميزة**: البريد يُنزّل إلى الجهاز المحلي
- **المنفذ**: 995 (SSL/TLS) أو 110 (غير مشفر)

---

## 📤 بروتوكول الإرسال (Outgoing Mail):

### SMTP (Simple Mail Transfer Protocol)
- **الاستخدام**: إرسال البريد الإلكتروني فقط
- **المنفذ**: 587 (TLS - موصى به) أو 465 (SSL) أو 25 (غير مشفر)
- **هذا ما نحتاجه لإرسال البريد من الموقع!**

---

## 🔧 إعدادات Office 365:

### للإرسال (SMTP) - ما نحتاجه:
```
SMTP Server: smtp.office365.com
Port: 587
Encryption: TLS
Username: no-reply@yasmarina.ae
Password: [كلمة المرور]
```

### للاستقبال (IMAP) - للبريد الوارد فقط:
```
IMAP Server: outlook.office365.com
Port: 993
Encryption: SSL/TLS
Username: no-reply@yasmarina.ae
Password: [كلمة المرور]
```

### للاستقبال (POP) - للبريد الوارد فقط:
```
POP Server: outlook.office365.com
Port: 995
Encryption: SSL/TLS
Username: no-reply@yasmarina.ae
Password: [كلمة المرور]
```

---

## ❓ هل يمكن استخدام IMAP/POP للإرسال؟

**لا، لا يمكن!**

- IMAP و POP **للاستقبال فقط**
- لإرسال البريد، **يجب استخدام SMTP**
- لكن يمكن استخدام **نفس بيانات الاعتماد** (username/password) مع SMTP

---

## ✅ الحل الصحيح:

### الطريقة الحالية (SMTP) - صحيحة ✅
```php
// في config.php
define('SMTP_HOST', 'smtp.office365.com');
define('SMTP_PORT', 587);
define('SMTP_USERNAME', 'no-reply@yasmarina.ae');
define('SMTP_PASSWORD', 'Apple@2025');
define('SMTP_SECURE', 'tls');
```

هذا هو الإعداد الصحيح لإرسال البريد!

---

## 🔄 إذا كنت تريد استخدام IMAP/POP:

### استخدام IMAP لقراءة البريد الوارد (اختياري):

إذا أردت قراءة البريد الوارد من Office 365 في PHP:

```php
<?php
// قراءة البريد الوارد باستخدام IMAP
$hostname = '{outlook.office365.com:993/imap/ssl}INBOX';
$username = 'no-reply@yasmarina.ae';
$password = 'Apple@2025';

$inbox = imap_open($hostname, $username, $password);

if ($inbox) {
    $emails = imap_search($inbox, 'ALL');
    
    if ($emails) {
        foreach ($emails as $email_number) {
            $header = imap_headerinfo($inbox, $email_number);
            echo "From: " . $header->fromaddress . "\n";
            echo "Subject: " . $header->subject . "\n";
            echo "Date: " . $header->date . "\n\n";
        }
    }
    
    imap_close($inbox);
} else {
    echo "Failed to connect to IMAP server";
}
?>
```

**ملاحظة:** هذا للقراءة فقط، ليس للإرسال!

---

## 📋 ملخص:

| البروتوكول | الاستخدام | المنفذ | Encryption |
|------------|----------|--------|------------|
| **SMTP** | إرسال البريد | 587 | TLS |
| IMAP | استقبال البريد | 993 | SSL/TLS |
| POP | استقبال البريد | 995 | SSL/TLS |

---

## 🎯 الخلاصة:

1. **لإرسال البريد من الموقع**: استخدم **SMTP** (كما هو حالياً) ✅
2. **لقراءة البريد الوارد**: استخدم **IMAP** أو **POP** (اختياري)
3. **نفس بيانات الاعتماد** تعمل مع جميع البروتوكولات
4. **المشكلة الحالية** ليست في البروتوكول، بل في **تفعيل SMTP Authentication**

---

## 🔧 الحل للمشكلة الحالية:

المشكلة ليست في البروتوكول (SMTP صحيح ✅)، بل في:

1. **تفعيل SMTP Authentication** في Office 365 Admin
2. أو استخدام **OAuth2** (الحل الدائم)

راجع: `MICROSOFT-365-AUTH-FIX.md`

---

## 📚 مراجع:

- [Microsoft Office 365 Email Settings](https://support.microsoft.com/en-us/office/pop-imap-and-smtp-settings-for-outlook-com-d088b986-291d-42b8-9564-9c414e2aa040)
- [Office 365 SMTP Settings](https://docs.microsoft.com/en-us/exchange/mail-flow-best-practices/how-to-set-up-a-multifunction-device-or-application-to-send-email-using-microsoft-365-or-office-365)

---

**الخلاصة:** استمر في استخدام SMTP (كما هو حالياً) - هذا صحيح! المشكلة في تفعيل SMTP Auth أو استخدام OAuth2.

