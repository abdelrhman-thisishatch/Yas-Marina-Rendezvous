# 🔧 حل مشكلة المصادقة مع Hostinger SMTP

## ❌ المشكلة:
```
SMTP Error: Could not authenticate.
```

## ✅ الحلول الممكنة:

### الحل 1: استخدام المنفذ 587 مع TLS (موصى به)

Hostinger يدعم بشكل أفضل المنفذ **587 مع TLS** بدلاً من 465 مع SSL.

**الإعدادات المحدثة:**
```php
SMTP_HOST: smtp.hostinger.com
SMTP_PORT: 587
SMTP_SECURE: tls
```

### الحل 2: التحقق من بيانات الاعتماد

1. **اسم المستخدم:**
   - يجب أن يكون البريد الإلكتروني الكامل: `no-reply@yasmarina.ae`
   - ليس فقط: `no-reply`

2. **كلمة المرور:**
   - تأكد من نسخ كلمة المرور بشكل صحيح
   - تحقق من وجود مسافات إضافية
   - بعض الأحرف الخاصة قد تحتاج إلى escape

### الحل 3: التحقق من الحساب في Hostinger

1. **تسجيل الدخول إلى Hostinger Control Panel**
2. **اذهب إلى Email Accounts**
3. **تحقق من:**
   - ✅ الحساب `no-reply@yasmarina.ae` موجود
   - ✅ الحساب نشط (Active)
   - ✅ كلمة المرور صحيحة
   - ✅ SMTP مفعل للحساب

### الحل 4: إعادة تعيين كلمة المرور

إذا استمرت المشكلة:
1. اذهب إلى Hostinger Control Panel
2. Email Accounts → no-reply@yasmarina.ae
3. Change Password
4. استخدم كلمة مرور جديدة بسيطة (بدون أحرف خاصة معقدة)
5. حدث `config.php` بالكلمة الجديدة

### الحل 5: استخدام App Password (إن أمكن)

بعض مزودي الاستضافة يوفرون App Password منفصلة:
1. ابحث عن "App Password" أو "Application Password" في Hostinger
2. استخدمها بدلاً من كلمة المرور العادية

---

## 🔍 خطوات التشخيص:

### 1. اختبار الاتصال:
افتح: `https://yasrendezvous.ae/test-smtp-connection.php`

### 2. فحص السجل:
تحقق من `email_log.txt` للأخطاء التفصيلية

### 3. التحقق من الإعدادات:
- ✅ Host: `smtp.hostinger.com`
- ✅ Port: `587` (أو `465`)
- ✅ Username: `no-reply@yasmarina.ae` (البريد الكامل)
- ✅ Password: صحيحة ومحدثة

---

## 📝 الإعدادات الموصى بها:

### Option 1: Port 587 with TLS (الأفضل)
```php
define('SMTP_HOST', 'smtp.hostinger.com');
define('SMTP_PORT', 587);
define('SMTP_SECURE', 'tls');
```

### Option 2: Port 465 with SSL
```php
define('SMTP_HOST', 'smtp.hostinger.com');
define('SMTP_PORT', 465);
define('SMTP_SECURE', 'ssl');
```

---

## ⚠️ ملاحظات مهمة:

1. **اسم المستخدم:**
   - ✅ صحيح: `no-reply@yasmarina.ae`
   - ❌ خاطئ: `no-reply`

2. **كلمة المرور:**
   - تأكد من عدم وجود مسافات إضافية
   - بعض الأحرف الخاصة قد تسبب مشاكل

3. **الحساب:**
   - يجب أن يكون الحساب نشطاً في Hostinger
   - SMTP يجب أن يكون مفعل

4. **المنفذ:**
   - جرب 587 أولاً (الأكثر توافقاً)
   - إذا لم يعمل، جرب 465

---

## 🆘 إذا استمرت المشكلة:

1. **اتصل بدعم Hostinger:**
   - تأكد من أن SMTP مفعل لحسابك
   - اسأل عن الإعدادات الصحيحة

2. **جرب حساب بريد آخر:**
   - أنشئ حساب بريد جديد في Hostinger
   - استخدمه للاختبار

3. **تحقق من Firewall:**
   - تأكد من أن المنفذ 587 أو 465 مفتوح

---

**آخر تحديث:** تم تحديث الإعدادات لاستخدام المنفذ 587 مع TLS

