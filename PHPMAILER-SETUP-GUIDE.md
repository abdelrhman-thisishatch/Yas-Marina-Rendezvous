# ✅ PHPMailer Setup Complete!

## 🎉 ما تم إنجازه:

### 1. ✅ تحميل وتثبيت PHPMailer
- تم تحميل أحدث إصدار من PHPMailer
- تم استخراج الملفات في مجلد `PHPMailer/`
- الملفات المثبتة:
  - ✅ PHPMailer.php
  - ✅ SMTP.php
  - ✅ Exception.php
  - ✅ OAuth.php (إضافي)
  - ✅ POP3.php (إضافي)

### 2. ✅ تحديث send-email-smtp.php
- يستخدم الآن إعدادات من `config.php`
- إضافة Rate Limiting (10 رسائل/ساعة)
- إضافة Logging لتتبع الأخطاء
- إضافة جميع الرسائل بالإنجليزية
- معالجة أخطاء محسّنة
- حماية من HTML injection

### 3. ✅ تحديث index.html
- تم تغيير النموذج ليستخدم `send-email-smtp.php`
- جاهز للاستخدام مباشرة

---

## 🔧 الإعدادات المطلوبة

### الخيار 1: استخدام Gmail SMTP (الأسهل)

#### الخطوة 1: إنشاء App Password في Gmail

1. اذهب إلى: https://myaccount.google.com/security
2. فعّل "2-Step Verification" (إذا لم يكن مفعلاً)
3. اذهب إلى: https://myaccount.google.com/apppasswords
4. اختر:
   - App: **Mail**
   - Device: **Other (Custom name)** → اكتب "Yas Marina Website"
5. اضغط **Generate**
6. **انسخ الكود** الذي يظهر (16 حرف مثل: xxxx xxxx xxxx xxxx)

#### الخطوة 2: تحديث config.php

افتح `config.php` وحدّث هذه الإعدادات:

```php
// SMTP Settings
define('SMTP_HOST', 'smtp.gmail.com');
define('SMTP_PORT', 587);
define('SMTP_USERNAME', 'your-email@gmail.com'); // ضع بريدك في Gmail
define('SMTP_PASSWORD', 'xxxx xxxx xxxx xxxx'); // الصق الكود من الخطوة 1
define('SMTP_SECURE', 'tls');
```

#### الخطوة 3: اختبر!

1. ارفع جميع الملفات إلى السيرفر (بما فيها مجلد PHPMailer)
2. افتح موقعك واملأ النموذج
3. يجب أن تصل الرسالة بنجاح! ✅

---

### الخيار 2: استخدام SMTP من cPanel

#### الخطوة 1: إنشاء حساب البريد

1. ادخل cPanel
2. اذهب إلى **Email Accounts**
3. اضغط **Create**
4. املأ:
   - Email: `no-reply`
   - Domain: `yasmarina.ae`
   - Password: [اختر كلمة مرور قوية]
5. اضغط **Create Account**

#### الخطوة 2: الحصول على إعدادات SMTP

1. في صفحة Email Accounts
2. اضغط **Connect Devices** بجانب البريد
3. ستجد:
   - **Outgoing Server**: mail.yasmarina.ae (أو mail.yourdomain.com)
   - **Port**: 587 (أو 465)
   - **Username**: no-reply@yasmarina.ae
   - **Password**: [كلمة المرور التي أنشأتها]
   - **Security**: TLS (أو SSL إذا كان Port 465)

#### الخطوة 3: تحديث config.php

```php
// SMTP Settings
define('SMTP_HOST', 'mail.yasmarina.ae'); // من cPanel
define('SMTP_PORT', 587); // أو 465
define('SMTP_USERNAME', 'no-reply@yasmarina.ae');
define('SMTP_PASSWORD', 'your-password-here'); // كلمة المرور من cPanel
define('SMTP_SECURE', 'tls'); // أو 'ssl' إذا كان Port 465
```

#### الخطوة 4: اختبر!

1. ارفع جميع الملفات
2. جرّب النموذج
3. يجب أن يعمل! ✅

---

## 📁 الملفات والمجلدات

### الملفات الرئيسية:
```
code/
├── PHPMailer/              ← المكتبة المثبتة (جديد!)
│   ├── PHPMailer.php
│   ├── SMTP.php
│   └── Exception.php
├── config.php              ← إعدادات SMTP
├── send-email-smtp.php     ← محدّث ليستخدم PHPMailer
├── index.html              ← محدّث ليستخدم send-email-smtp.php
└── email_log.txt           ← سجل العمليات
```

---

## 🔍 استكشاف الأخطاء

### المشكلة: "PHPMailer Error: SMTP connect() failed"

**الحلول:**
1. تأكد من أن Username و Password صحيحين
2. تأكد من أن Port صحيح (587 أو 465)
3. إذا كنت تستخدم Gmail:
   - تأكد من أنك استخدمت App Password وليس كلمة المرور العادية
   - تأكد من تفعيل 2-Step Verification
4. تحقق من email_log.txt للتفاصيل

### المشكلة: "SMTP Authentication failed"

**الحلول:**
1. تأكد من صحة SMTP_USERNAME و SMTP_PASSWORD
2. إذا كنت تستخدم cPanel، تأكد من أن البريد موجود
3. جرّب Port مختلف (587 بدلاً من 465 أو العكس)

### المشكلة: لا يصل البريد

**الحلول:**
1. تحقق من مجلد Spam/Junk
2. افتح email_log.txt وابحث عن آخر خطأ
3. تأكد من أن RECIPIENT_EMAIL في config.php صحيح

---

## 🎯 التوصيات

### ✅ الإعدادات الموصى بها في config.php:

```php
// For Gmail (Most Reliable)
define('SMTP_HOST', 'smtp.gmail.com');
define('SMTP_PORT', 587);
define('SMTP_USERNAME', 'your-email@gmail.com');
define('SMTP_PASSWORD', 'your-app-password'); // NOT regular password!
define('SMTP_SECURE', 'tls');

// Enable logging for debugging
define('LOG_EMAILS', true);
define('DEBUG_MODE', false); // Set to true only when debugging

// Recipient
define('RECIPIENT_EMAIL', 'samer.eladem@yasmarina.ae');
```

---

## 🔐 ملاحظات أمنية

### ⚠️ مهم جداً:

1. **لا تشارك config.php علناً** - يحتوي على كلمات مرور
2. **استخدم .gitignore** لإخفاء config.php من Git:
   ```
   config.php
   email_log.txt
   ```
3. **غيّر DEBUG_MODE إلى false** في الإنتاج
4. **استخدم HTTPS** على موقعك
5. **احذف ملفات الاختبار**:
   - ❌ diagnose-email.php
   - ❌ test-email.php
   - ❌ SIMPLE-FIX.html

---

## 📊 المزايا المضافة

### ما يتميز به send-email-smtp.php الآن:

1. ✅ **SMTP Authentication** - أكثر موثوقية من mail()
2. ✅ **Rate Limiting** - حماية من Spam (10 رسائل/ساعة)
3. ✅ **Logging** - تتبع كل عملية في email_log.txt
4. ✅ **Input Validation** - فحص البيانات قبل الإرسال
5. ✅ **HTML Protection** - حماية من HTML injection
6. ✅ **UTF-8 Support** - دعم العربية والإنجليزية
7. ✅ **Error Handling** - معالجة الأخطاء بشكل احترافي
8. ✅ **Config Integration** - كل الإعدادات من ملف واحد

---

## ✨ الخطوات التالية

### 1. الآن - اختبر على localhost أو test server:
```bash
☐ حدّث config.php بإعدادات Gmail
☐ ارفع جميع الملفات
☐ اختبر النموذج
☐ تحقق من email_log.txt
```

### 2. قبل النشر على Live:
```bash
☐ غيّر DEBUG_MODE إلى false
☐ تأكد من صحة RECIPIENT_EMAIL
☐ اختبر مرة أخرى
☐ احذف ملفات الاختبار
```

### 3. بعد النشر:
```bash
☐ راقب email_log.txt
☐ اختبر من متصفحات مختلفة
☐ تحقق من وصول البريد
```

---

## 💡 نصائح إضافية

### لتحسين معدل وصول البريد:

1. **استخدم Gmail SMTP** في البداية (أكثر موثوقية)
2. **أضف SPF و DKIM records** في DNS settings
3. **استخدم بريد من نفس الدومين** كـ From address
4. **تجنب الكلمات المشبوهة** في subject و message

### للأداء الأفضل:

1. قلل عدد الطلبات بـ Rate Limiting
2. استخدم CDN للملفات الثابتة
3. فعّل caching في PHP
4. قلل حجم email_log.txt دورياً

---

## 📞 الدعم

إذا واجهت أي مشكلة:

1. **افتح email_log.txt** وانسخ آخر 10 أسطر
2. **تحقق من إعدادات SMTP** في config.php
3. **جرّب Gmail SMTP أولاً** قبل cPanel
4. **أرسل لي**:
   - محتوى email_log.txt
   - screenshot من الخطأ
   - إعدادات SMTP (بدون Password!)

---

## ✅ قائمة التحقق النهائية

```
☐ PHPMailer مثبت في مجلد PHPMailer/
☐ config.php محدّث بإعدادات SMTP الصحيحة
☐ send-email-smtp.php موجود ومحدّث
☐ index.html يستخدم send-email-smtp.php
☐ تم اختبار إرسال رسالة تجريبية
☐ البريد وصل بنجاح
☐ email_log.txt يظهر "Email sent successfully"
☐ تم حذف ملفات الاختبار غير الضرورية
```

---

## 🎊 مبروك!

إذا وصلت إلى هنا وكل شيء يعمل، فأنت جاهز! 🚀

**النظام الآن:**
- ✅ يستخدم PHPMailer مع SMTP
- ✅ موثوق 100%
- ✅ محمي ضد Spam
- ✅ يسجل كل العمليات
- ✅ احترافي وآمن

**استمتع بنظام البريد الجديد!** 📧

---

**آخر تحديث:** 5 نوفمبر 2025  
**الإصدار:** 3.0 with PHPMailer  
**الحالة:** ✅ جاهز للاستخدام

